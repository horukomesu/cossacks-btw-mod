План расширения разрешений и оконного режима

1) Разблокировать высокие режимы (>= 2560x1440) в перечислении видеорежимов.
   - Убрать ограничение 1920 по ширине в `src/Main executable/Ddini.cpp` (ModeCallback).
   - Добавить 2560x1440 в список кандидатов для оконного режима в `CreateDDObjects()`.
   СТАТУС: выполнить предстоит

2) Убрать принудительный откат меню к 1024x768 в оконном режиме.
   - Удалить вызовы `SetGameDisplayModeAnyway(1024, 768)` для меню при `window_mode` в `src/Main executable/Interface.cpp` и оставлять текущее разрешение.
   - Вместо этого центрировать окно (`ResizeAndCenterWindow`).
   СТАТУС: выполнить предстоит

3) Делать позиционирование меню относительно текущего разрешения.
   - Всегда вычислять `menu_x_off`/`menu_y_off` от `RealLx`/`RealLy`, а не только в полноэкранном режиме.
   СТАТУС: выполнить предстоит

4) Снять ограничение 1920x1080 на стартовый полноэкранный режим.
   - Удалить кап `screen_width = 1920; screen_height = 1080;` в `doInit()` (`src/Main executable/Ddex1.cpp`).
   СТАТУС: выполнить предстоит

5) Проверка и доводка оконного режима.
   - После смены разрешения в окне вызывать `ResizeAndCenterWindow()`.
   - Убедиться, что список доступных режимов в окне ограничен физическим экраном.
   СТАТУС: выполнить предстоит

6) Сборка/прогон: пересобрать проект и проверить запуск в 2560x1440 в fullscreen и window/borderless.
   СТАТУС: ожидает выполнения локальной сборки.


